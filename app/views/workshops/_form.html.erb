 <%= form_for @workshop, :html => {:class => "form-horizontal center"} do |f| %>
  <% if @workshop.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@workshop.errors.count, "error") %> prohibited
        this workshop from being saved:
      </h2>
      <ul>
        <% @workshop.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
        <%= f.label :name, "اسم الورشه:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :name, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
       <%= f.label "المنطقه:", class: "col-md-4 control-label" %>
       <div class="col-md-8">
          <%= f.select(:location_id, @location_options)%>
        </div>
      </div>
      <%= f.submit "حفظ", class: "btn btn-default btn-primary" %>   
 <% end %>