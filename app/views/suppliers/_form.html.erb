 <%= form_for @supplier, :html => {:class => "form-horizontal center",:dir=>"rtl"} do |f| %>
  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@supplier.errors.count, "error") %> prohibited
        this supplier from being saved:
      </h2>
      <ul>
        <% @supplier.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group" dir="rtl">
        <%= f.label "اسم المورد:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :name, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label  "العنوان:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :address, class: "form-control" %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label "التليفون:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :phone, class: "form-control" %>
        </div>
      </div>
       <div class="form-group">
        <%= f.label "المسئول:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :person, class: "form-control" %>
        </div>
      </div>
       <div class="form-group">
        <%= f.label "تليفون المسئول:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :person_phone, class: "form-control" %>
        </div>
      </div>
        <div class="form-group">
        <%= f.label "الفئه:", class: "col-md-4 control-label" %>
        <div class="col-md-8">
          <%= f.text_field :category, class: "form-control" %>
        </div>
      </div>
  
      <%= f.submit "Submit", class: "btn btn-default btn-primary" %>   
 <% end %>