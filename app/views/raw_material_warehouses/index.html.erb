 <div class="page-header">
    <h1>المخازن</h1>

 
</div>

<table class="table table-striped table-bordered table-condensed">
<thead>
<tr>
  <th colspan="1" class="center">كود الصنف</th>
  <th colspan="1" class="center">الصنف</th>
  <th colspan="1" class="center">الوصف</th>
  <th colspan="1" class="center">الوحده</th>
  <th colspan="1" class="center">حد الامان</th>
  <th colspan="1" class="center">الفئه</th>

  <th colspan="1" class="action">Actions</th>
  <th></th>
</tr>
</thead>
<tbody>
    <% unless @rawmaterials.empty? %>
    <% @rawmaterials.each do |rawmaterial| %>
      <tr>
        <td><%= rawmaterial.code %></td>
        <td><%= rawmaterial.item %></td>
        <td><%= rawmaterial.description %></td>
        <td><%= rawmaterial.uom %></td>
        <td><%= rawmaterial.safety_level %></td> 
        <td><%= rawmaterial.category%></td>        
       
        <td><%= link_to 'تعديل', edit_raw_material_warehouse_path(rawmaterial) %>
        <%= link_to 'مسح', raw_material_warehouse_path(rawmaterial),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>

          <!-- This currently works but smells -->
        </td>
        <td></td>
      </tr>
    <% end %>
    <% end %>

</tbody>
</table>
 <div class="contextual">
    <%= link_to(new_raw_material_warehouse_path, class: "btn btn-success") do %>
      اضافه ماده خام<i class="icon-plus icon-white"></i>
    <% end %>
  </div>
